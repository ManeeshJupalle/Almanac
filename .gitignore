# Local-only docs (kept on disk, not published to the repo)
BUILD_PHASES.md
BUILD_PHASES_V2.md
AUDIT.md

# Rust
/target
**/target
# Cargo.lock is tracked on purpose: Almanac ships a binary app.

# Node / frontend
node_modules
dist
dist-ssr
.vite

# Tauri
src-tauri/target
src-tauri/gen

# On-device model files (fetched once; see README) — too big for git
models/

# Local database & anything with real data
*.db
*.db-journal
*.sqlite
*.sqlite3

# Secrets / OAuth — CRITICAL, these must NEVER be committed
.env
.env.*
!.env.example
# Unredacted API captures (real content) — local only, never committed
.fixtures-raw/
*.local
/fixtures/**/secrets*
client_secret*.json
credentials*.json
token*.json
tokens/
*.pem

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea
*.swp
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vscode/*
!.vscode/extensions.json
